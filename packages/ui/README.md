# ImmersionKit UI Package

`packages/ui` contains shared React UI components and screens used by extension
surfaces.

Use it for local component development and extension screen previews:

```bash
pnpm --filter @immersionkit/ui dev
pnpm --filter @immersionkit/ui build
```

The package is internal to the workspace and is not published separately.

